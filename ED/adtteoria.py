"""
ADT: Abstract Data Type
-Diseñado por el programador
-Basado en los tipos de dato basicos
-Tienen una estructura bien establecida
-Tiene un número de operaciones

ADT Arreglo
-Un adt que almacena valores del mismo tipo, pero diferenciados por un índice
Operaciones: Array(n)
             length()
             get_item(index)
             set_item(index,value)
             clearing(value)


ADT Array 2D
Es un adt que almacena valores del mismo tipo agrupados en renglones y columnas,
similar a una tabla de excel
Operaciones: Array2D(rows,cols)
             get_num_rows()
             get_num_cols()
             claring(value)
             set_item(row,col,value)
             get_item(row,col)
             to_string()

